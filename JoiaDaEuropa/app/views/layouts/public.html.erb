<!DOCTYPE html>
<html>
<head> <head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="description" content="">
  <meta name="author" content="">
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  <link rel="shortcut icon" href="http://getbootstrap.com/assets/ico/favicon.ico">

  <title>JoiaDaEuropa</title>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <link href="./bootstrap/css/bootstrap.min.css" rel="stylesheet">
  <link href="./bootstrap/css/header.css" rel="stylesheet">
</head>
    <title>JoiaDaEuropa</title>
    <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>
<body>

<!-- Navbar (sit on top) -->
<div class="w3-right w3-hide-small">
  <%= link_to 'PT', '?lang=pt' %>
  <span>|</span>
  <%= link_to 'EN', '?lang=en' %>
</div>
    <div class="w3-bar w3-white w3-wide w3-padding w3-card-2 w3-bar-fixed-top" role="navigation">
        <%= image_tag('logo.png', :alt => 'logo feed') %>
        <!-- Float links to the right. Hide them on small screens -->
        <div class="w3-right w3-hide-small">
            <a href="<%= home_index_path %>#projects" class="w3-bar-item w3-button">Mission</a>
            <a href="<%= home_index_path %>#about" class="w3-bar-item w3-button">About Us</a>
            <a href="<%= home_index_path %>#contact" class="w3-bar-item w3-button">Contacts</a>
            <a href="<%= home_index_path %>#news" class="w3-bar-item w3-button">News</a>

            <% if current_user %>
              <% @accounts = User.where(id: current_user.id) %>

              <% if @accounts.find_by(is_client: '1') %>
                  <%= link_to 'Client Area', client_area_index_path, :class => 'w3-bar-item w3-button' %>

              <% else %>
                  <%= link_to 'Agent Area', agent_area_index_path, :class => 'w3-bar-item w3-button' %>
              <% end %>

                <%= link_to 'Sign Out', destroy_user_session_path, {method: 'delete', class: 'w3-bar-item w3-button'} %>
            <% else %>
                <%= link_to 'Sign In', new_user_session_path, :class => 'w3-bar-item w3-button' %>
            <% end %>

        </div>
    </div>
</div>

<div class="container"></div>

<%= yield %>
</body>

</html>
