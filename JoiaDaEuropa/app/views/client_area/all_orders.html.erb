<div class="container-fluid">
    <div class="row content">
        <div class="col-sm-3 sidenav hidden-xs">
            <ul class="nav nav-pills nav-stacked">
                <li class="active">Menu</li>
                <li><%= link_to t('Perfil'), profile_index_path %></li>
                <li><%= link_to t('Nova Encomenda'), client_area_new_order_path %></li>
                <li><%= link_to t('Encomendas'), client_area_all_orders_path %></li>
                <li><a href="#">Contacte-nos</a></li>
            </ul>
        </div>

        <br>
        <div class="col-sm-3">
            <table id="example" class="table table-striped table-bordered" cellspacing="0" width="100%">
                <tr>
                    <th>IDEncomenda</th>
                    <th>Data de Entrega</th>
                    <th>Preço</th>
                    <th>Obs</th>
                    <th>Estado</th>
                </tr>
                <% @orders.each do |order| %>
                    <tr>
                        <td><%= link_to order.id, client_area_view_order_path(order.id) %></td>
                        <td><%= h order.delivery_date %></td>
                        <td><%= h order.price %></td>
                        <td><%= h order.obs %></td>
                        <td><%= h order.order_state_id %></td>
                <% end %>
                </tr>

            </table>
        </div>
    </div>
</div>


